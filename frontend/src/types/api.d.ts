/**
 * ここは自動生成される。手で編集しないこと。
 *
 *   npm run gen:api   （バックエンドを起動した状態で実行）
 *
 * DRF の drf-spectacular が吐く OpenAPI から openapi-typescript が型を生成する。
 * CI ではスキーマ差分を検出して、フロントとバックの型ズレを落とす（設計書 第10.1章）。
 */
export {}
